# Voice files

Place a Piper `.onnx` model and its matching `.onnx.json` configuration in this
directory. The default configuration expects `en_US-lessac-medium.onnx`.

Model files are intentionally not versioned and are not included in the PyPI
wheel.